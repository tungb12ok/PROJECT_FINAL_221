@page
@model WEB.Pages.Admin.ManagerFinanciTransaction.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].TransactionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].TransactionDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FinancialTransaction[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FinancialTransaction) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TransactionDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.TransactionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.TransactionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.TransactionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
